// dashboard/layout.js

import AppLayout from "../app-layout"; // Path may vary based on structure

export default function PaymentLayout({ children }) {
    return <AppLayout>{children}</AppLayout>;
}
